[package]
name = "zarrs_jl"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
zarrs = { version = "0.23", features = ["filesystem"] }
once_cell = "1"
serde_json = "1"

[profile.release]
lto = true
